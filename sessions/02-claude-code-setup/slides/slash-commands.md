# Slash Commands Worth Knowing

<div class="two-col">
<div>

**Inspect / configure**
- `/status` — auth, version, model
- `/config` — settings UI
- `/context` — token usage breakdown
- `/usage` — cost & usage stats
- `/permissions` — view & edit rules

**Tune the model**
- `/model` — pick mid-session  *(Sonnet 4.6 ↔ Opus 4.7)*
- `/effort` — dial reasoning effort

</div>
<div>

**Look-and-feel & misc**
- `/theme` — colour scheme
- `/memory` — open & edit CLAUDE.md
- `/plugin` — install/manage plugins
- `/help`, `/compact`, `/init`

**Hidden gem**
- `/powerup` — 10 short interactive lessons

</div>
</div>

*Session-navigation commands — `/clear`, `/rewind`, `/resume`, `/fork`, `/rename` — were covered earlier.*

---

## Try it now — `/powerup`

Run `/powerup` → pick a lesson → try it.

Suggested picks for sci-dev *(topics not yet covered today)*:

- *Run in the background* — tasks
- *Extend with tools* — MCP
- *Multiply yourself* — subagents
- *Code from anywhere* — remote control

Note:
*Automate your workflow* (skills + hooks) is the natural bridge into the next section, so don't suggest that one — they're about to see it anyway.
